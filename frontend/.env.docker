VITE_API_BASE_URL=http://backend:8000

# Must match CYBER_SURAKSHYA_API_KEY passed to the backend container.
# Supply it at build time rather than committing a value:
#   docker compose build --build-arg VITE_API_KEY="$CYBER_SURAKSHYA_API_KEY"
VITE_API_KEY=
